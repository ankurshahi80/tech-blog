<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>The Tech Blog</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
</head>
<body>
    <div class="wrapper">
        <header class="hero">
            <nav>
                {{#if loggedIn}}
                    <a class="nav-menu" href = "/">Home</a>
                    <a class="nav-menu" href = "/dashboard">Dashboard</a>
                    <button id="logout" class="btn-no-style nav-menu">Logout</button>
                {{else}}
                    <a class="nav-menu" href = "/">Home</a>
                    <a class="nav-menu" href = "/dashboard">Dashboard</a>
                    <a class="nav-menu" href="/login">Login</a>
                    <button id="logout" class="btn-no-style nav-menu">Logout</button>
                {{/if}}    
            </nav>
            <h1 class = "app-title">
                {{#if dashboardWindow}}
                Your Dashboard
                {{else}}
                The Tech Blog
                {{/if}}
            </h1>
        </header>
        <main>
            {{{body}}}
        </main>
        <footer>
            
        </footer>
    </div>
    {{#if loggedIn}}
    <script src="/javascript/logout.js"></script>
    {{/if}}
</body>
</html>